# Set up a consistent logger.
import logging


def get_logger(name: str, level: int = logging.INFO):
    """
    Configure and return a logger that writes to stdout with timestamps.
    Avoids adding duplicate handlers if called multiple times.
    """
    # TODO: create logger, set level, and attach handler with formatter
    pass
